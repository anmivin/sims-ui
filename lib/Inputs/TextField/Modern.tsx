import React from "react";

import { TextfieldProps } from "./TextField.types";
import { ModernTextfield } from "./TextField.styled";

export const TextFieldModern = (props: TextfieldProps) => {
  return <ModernTextfield {...props} />;
};
